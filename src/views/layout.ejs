<!DOCTYPE html>
<html lang="en">
  <head>
    <title><%= title %></title>
    <link rel="icon" href="/nodejs-icon.svg" type="image/x-icon">
    <link rel="stylesheet" href="/reset.css">
    <link rel="stylesheet" href="/style.css">
    <link rel="stylesheet" href="/outside-routes.css">
  </head>
  <body>
    <header>
      <h1>
        <span>File Uploader</span>
        <a href="https://github.com/endulum/odin-uploader" target="_blank">
          <img class="github" src="/assets/github.svg" alt="Link to this app's repository">
        </a>
      </h1>
      <div class="links">
        <% if (locals.user) { %>
          <p>
            Logged in as <b><%= locals.user.username %></b>
          </p>
          <nav>
            <a href="/">Home directory</a>
            <a href="/logout">Log out</a>
          </nav>
        <% } else { %>
          <p>
            Not logged in
          </p>
          <nav>
            <a href="login">Log in</a>
            <a href="/signup">Sign up</a> 
          </nav>
        <% } %>
      </div>
    </header>
    <main>
      <% if (locals.alert.length > 0) { %>
        <p class="alert"><%= locals.alert[0] %></p>
      <% } %>
      <h2><%= title %></h2>
      <%- include(`${page}`) %>
    </main>
  </body>
</html>