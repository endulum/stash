<label for="location">Select location</label>
  <div class="input">
    <select 
      name="location" 
      id="location" 
    >
      <% folderTree.forEach(location => { %>
        <option value=<%= location.id %>>
          <%= location.name %>
        </option>
      <% }) %>
    </select>
  </div>